import React from 'react';
import {StyleSheet, Text, View} from 'react-native';

const Nas = () => {
  return (
    <View>
      <Text>Nas</Text>
    </View>
  );
};

export default Nas;

const styles = StyleSheet.create({});
